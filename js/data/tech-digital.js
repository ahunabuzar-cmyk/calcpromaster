// tech digital
const TECH_TOOLS = [
  { id: 'internet-speed', name: 'Internet Download Time', desc: 'Calculate file download time', kw: 'download time calculator', inputs: [{id:'fileSize',label:'File Size (GB)',type:'number',def:10},{id:'speed',label:'Speed (Mbps)',type:'number',def:100}], calc: function(v) { const sec = v.fileSize*8000/v.speed; const min = sec/60; return { result: Math.floor(min) + 'm ' + Math.round((min-Math.floor(min))*60) + 's', chart: null, extra: v.fileSize + ' GB at ' + v.speed + ' Mbps | ' + sec.toFixed(0) + ' seconds total' }; } },
  { id: 'bandwidth-calc', name: 'Bandwidth Calculator', desc: 'Calculate bandwidth needed for activities', kw: 'free bandwidth calculator', inputs: [{id:'devices',label:'Devices',type:'number',def:5},{id:'activity',label:'Activity',type:'select',opts:[{v:'streaming',l:'Streaming 4K'},{v:'gaming',l:'Gaming'},{v:'browsing',l:'Browsing'}],def:'streaming'}], calc: function(v) { const perDevice = {streaming:25,gaming:10,browsing:3}; const total = v.devices * (perDevice[v.activity]||10); return { result: total + ' Mbps needed', chart: null, extra: v.devices + ' devices | Per device: ' + (perDevice[v.activity]||10) + ' Mbps | ' + v.activity }; } },
  { id: 'data-usage', name: 'Data Usage Calculator', desc: 'Calculate monthly data usage', kw: 'data usage calculator', inputs: [{id:'streamingHrs',label:'Streaming (hrs/day)',type:'number',def:2},{id:'browsingHrs',label:'Browsing (hrs/day)',type:'number',def:4},{id:'gamingHrs',label:'Gaming (hrs/day)',type:'number',def:1}], calc: function(v) { const totalGB = v.streamingHrs*3 + v.browsingHrs*0.5 + v.gamingHrs*1.5; return { result: (totalGB*30).toFixed(0) + ' GB/month', chart: null, extra: 'Streaming: ' + (v.streamingHrs*3*30).toFixed(0) + ' GB | Browse: ' + (v.browsingHrs*0.5*30).toFixed(0) + ' GB | Gaming: ' + (v.gamingHrs*1.5*30).toFixed(0) + ' GB' }; } },
  { id: 'video-size', name: 'Video File Size', desc: 'Calculate video file sizes', kw: 'video file size', inputs: [{id:'duration',label:'Duration (min)',type:'number',def:10},{id:'bitrate',label:'Bitrate (Mbps)',type:'number',def:50},{id:'resolution',label:'Resolution',type:'select',opts:[{v:'1080p',l:'1080p'},{v:'4k',l:'4K'}],def:'1080p'}], calc: function(v) { const rate = v.resolution==='4k' ? v.bitrate : v.bitrate/2; const size = v.duration*60*rate/8/1024; return { result: size.toFixed(1) + ' GB', chart: null, extra: 'Duration: ' + v.duration + ' min | Bitrate: ' + rate + ' Mbps | ' + v.resolution }; } },
  { id: 'monitor-distance', name: 'Monitor Viewing Distance', desc: 'Find ideal screen distance', kw: 'monitor viewing distance', inputs: [{id:'screenSize',label:'Screen Size (in)',type:'number',def:27},{id:'resolution',label:'Resolution',type:'select',opts:[{v:'1080p',l:'1080p'},{v:'1440p',l:'1440p (QHD)'},{v:'2160p',l:'4K UHD'}],def:'1080p'}], calc: function(v) { const rec = {'1080p':{min:v.screenSize*1.5,ideal:v.screenSize*2.5},'1440p':{min:v.screenSize*1.2,ideal:v.screenSize*2},'2160p':{min:v.screenSize*1,ideal:v.screenSize*1.5}}; const r = rec[v.resolution]||rec['1080p']; return { result: 'Ideal: ' + r.ideal.toFixed(0) + ' in (' + (r.ideal*2.54).toFixed(0) + ' cm)', chart: null, extra: 'Min distance: ' + r.min.toFixed(0) + ' in | Resolution: ' + v.resolution + ' | Screen: ' + v.screenSize + '"' }; } },
  { id: 'wifi-channels', name: 'WiFi Channel Finder', desc: 'Find best WiFi channel', kw: 'wifi channel finder', inputs: [{id:'band',label:'Band',type:'select',opts:[{v:'2.4',l:'2.4 GHz'},{v:'5',l:'5 GHz'}],def:'2.4'},{id:'neighbors',label:'Nearby Networks',type:'number',def:5}], calc: function(v) { const ch = v.band==='2.4' ? [1,6,11] : [36,40,44,48,149,153,157,161]; return { result: 'Best channel: ' + ch[0], chart: null, extra: v.band + ' GHz | ' + ch.length + ' channels | ' + v.neighbors + ' nearby nets' + (v.band==='2.4'?' | Use channels 1, 6, or 11':'') }; } },
  { id: 'cloud-storage', name: 'Cloud Storage Cost', desc: 'Compare cloud storage plans', kw: 'cloud storage comparison', inputs: [{id:'storageGB',label:'Storage Needed (GB)',type:'number',def:100}], calc: function(v) { const p = [{n:'Google Drive',c:1.99,l:100},{n:'Dropbox',c:11.99,l:2000},{n:'iCloud',c:0.99,l:50},{n:'OneDrive',c:1.99,l:100}]; const best = p.reduce((a,b)=>(b.c/b.l)<(a.c/a.l)?b:a); return { result: 'Best: ' + best.n + ' ($' + best.c.toFixed(2) + '/' + best.l + 'GB)', chart: null, extra: p.map(x => x.n + ': $' + x.c + '/' + x.l + 'GB').join(' | ') }; } },
  { id: 'website-cost', name: 'Website Dev Cost', desc: 'Estimate website development cost', kw: 'website development cost', inputs: [{id:'type',label:'Project Type',type:'select',opts:[{v:'landing',l:'Landing Page'},{v:'business',l:'Business Site'},{v:'ecommerce',l:'E-commerce'},{v:'webapp',l:'Web App'},{v:'mobile',l:'Mobile App'}],def:'business'}], calc: function(v) { const costs = {landing:{s:1500,m:3000,c:6000},business:{s:5000,m:10000,c:25000},ecommerce:{s:15000,m:35000,c:75000},webapp:{s:25000,m:60000,c:150000},mobile:{s:30000,m:70000,c:200000}}; const c = costs[v.type]||costs.business; return { result: '$' + c.s.toLocaleString() + ' to $' + c.c.toLocaleString(), chart: null, extra: 'Simple: $' + c.s.toLocaleString() + ' | Medium: $' + c.m.toLocaleString() + ' | Complex: $' + c.c.toLocaleString() + ' | 2-6 months' }; } },
  { id: 'ssd-vs-hdd', name: 'SSD vs HDD Speed', desc: 'Compare SSD vs HDD speeds', kw: 'ssd vs hdd', inputs: [{id:'fileSize',label:'File Size (GB)',type:'number',def:50}], calc: function(v) { const ssd = v.fileSize/500*60; const hdd = v.fileSize/150*60; return { result: 'SSD: ' + Math.round(ssd) + 's | HDD: ' + Math.round(hdd) + 's', chart: null, extra: 'SSD: ~500 MB/s read | HDD: ~150 MB/s read | ' + v.fileSize + ' GB file | SSD is ' + Math.round(hdd/ssd) + 'x faster' }; } },
  { id: 'battery-life-estimator', name: 'Battery Life Estimator', desc: 'Estimate device battery life', kw: 'battery life estimator', inputs: [{id:'batteryMah',label:'Battery (mAh)',type:'number',def:4000},{id:'usageW',label:'Usage (watts)',type:'number',def:5},{id:'batteryV',label:'Battery Voltage (V)',type:'number',def:3.7}], calc: function(v) { const wh = v.batteryMah*v.batteryV/1000; const hrs = wh/v.usageW; return { result: hrs.toFixed(1) + ' hours', chart: null, extra: 'Battery: ' + v.batteryMah + ' mAh | Power: ' + v.usageW + 'W | ' + wh.toFixed(1) + ' Wh capacity' }; } },
  { id: 'screen-resolution', name: 'Screen Resolution Guide', desc: 'Find ideal resolution for screen size', kw: 'screen resolution guide', inputs: [{id:'screenSize',label:'Screen Size (in)',type:'number',def:24},{id:'distance',label:'Viewing Distance (ft)',type:'number',def:2.5}], calc: function(v) { const cm = v.distance*30.48; const ppd = 2*v.screenSize*2.54*Math.tan(Math.PI/180)*cm; const rec = ppd > 60 ? '4K' : ppd > 30 ? '1440p' : '1080p'; return { result: 'Recommended: ' + rec, chart: null, extra: v.screenSize + '" at ' + v.distance + 'ft | PPD: ' + ppd.toFixed(0) + ' | ' + (rec==='4K'?'✓ Sharp':rec==='1440p'?'Good balance':'HD is fine') }; } },
  { id: 'cable-length', name: 'Cable Length Calculator', desc: 'Calculate optimal cable length', kw: 'cable length calculator', inputs: [{id:'distance',label:'Distance (m)',type:'number',def:10},{id:'cableType',label:'Cable Type',type:'select',opts:[{v:'hdmi',l:'HDMI'},{v:'ethernet',l:'Ethernet'},{v:'usb',l:'USB'}],def:'hdmi'}], calc: function(v) { const max = {hdmi:15,ethernet:100,usb:5}; const ok = v.distance <= (max[v.cableType]||15); return { result: (ok?'✓ All good':'⚠ Signal issues likely') + ' at ' + v.distance + 'm', chart: null, extra: 'Max length: ' + (max[v.cableType]||15) + 'm | Cable: ' + v.cableType + ' | ' + (ok?'Standard range':'Consider signal booster/repeater') }; } },
  { id: 'ppi-calc', name: 'PPI (Pixels Per Inch)', desc: 'Calculate screen pixel density', kw: 'free ppi calculator', inputs: [{id:'widthPx',label:'Width (pixels)',type:'number',def:3840},{id:'heightPx',label:'Height (pixels)',type:'number',def:2160},{id:'diagonal',label:'Diagonal (in)',type:'number',def:27}], calc: function(v) { const ppi = Math.sqrt(v.widthPx*v.widthPx+v.heightPx*v.heightPx)/v.diagonal; return { result: Math.round(ppi) + ' PPI', chart: null, extra: 'Resolution: ' + v.widthPx + 'x' + v.heightPx + ' | ' + v.diagonal + '" screen | ' + (ppi>300?'Retina quality':ppi>200?'Good':'Standard') + ' | Pixel pitch: ' + (25.4/ppi).toFixed(2) + ' mm' }; } },
  { id: 'wattage-psu', name: 'Power Supply Calculator', desc: 'Calculate PSU wattage needed', kw: 'power supply wattage', inputs: [{id:'cpuTdp',label:'CPU TDP (watts)',type:'number',def:125},{id:'gpuTdp',label:'GPU TDP (watts)',type:'number',def:350},{id:'ramSticks',label:'RAM Sticks',type:'number',def:4},{id:'drives',label:'Drives',type:'number',def:3}], calc: function(v) { const total = v.cpuTdp+v.gpuTdp+v.ramSticks*7+v.drives*15+50; return { result: total + ' W (recommend: ' + Math.ceil(total*1.25/50)*50 + 'W PSU)', chart: null, extra: 'CPU: ' + v.cpuTdp + 'W | GPU: ' + v.gpuTdp + 'W | RAM: ' + v.ramSticks*7 + 'W | Drives: ' + v.drives*15 + 'W | 80% load target' }; } },
  { id: 'print-resolution', name: 'Print Resolution Guide', desc: 'Calculate optimal print resolution', kw: 'print resolution calculator', inputs: [{id:'widthPx',label:'Width (pixels)',type:'number',def:3000},{id:'heightPx',label:'Height (pixels)',type:'number',def:2000},{id:'dpi',label:'DPI',type:'select',opts:[{v:'150',l:'150 (Newspaper)'},{v:'300',l:'300 (Photo)'},{v:'600',l:'600 (Magazine)'}],def:'300'}], calc: function(v) { const dpi = parseInt(v.dpi); const w = v.widthPx/dpi; const h = v.heightPx/dpi; return { result: w.toFixed(1) + 'x' + h.toFixed(1) + ' inches', chart: null, extra: 'At ' + v.dpi + ' DPI | ' + (w*2.54).toFixed(0) + 'x' + (h*2.54).toFixed(0) + ' cm | MP: ' + (v.widthPx*v.heightPx/1000000).toFixed(1) + 'MP' }; } },
  { id: 'password-time', name: 'Password Crack Time', desc: 'Estimate how long it takes to brute-force a password', kw: 'password crack time calculator', inputs: [{id:'length',label:'Password Length',type:'number',def:12},{id:'charset',label:'Character Set',type:'select',opts:[{v:'digits',l:'Numbers (10)'},{v:'lower',l:'Lowercase (26)'},{v:'upperLower',l:'Upper+Lower (52)'},{v:'all',l:'All (95)'}],def:'all'},{id:'guesses',label:'Guesses per Second (M)',type:'number',def:10}], calc: function(v) { const pool = {digits:10,lower:26,upperLower:52,all:95}[v.charset]||95; const combos = Math.pow(pool, v.length); const sec = combos/(v.guesses*1e6); const years = sec/31557600; return { result: years < 1 ? (sec<60?Math.max(1,sec).toFixed(0)+' seconds':(sec/3600).toFixed(1)+' hours') : years.toLocaleString() + ' years', chart: null, extra: 'Entropy: ' + Math.round(v.length*Math.log2(pool)) + ' bits | Combos: ' + combos.toExponential(0) + (years>10000?' | Unbreakable by brute force ✓':' | Weak against clusters') }; } },
  { id: 'image-file-size', name: 'Image File Size Estimator', desc: 'Estimate image file size from dimensions', kw: 'image file size', inputs: [{id:'width',label:'Width (px)',type:'number',def:4000},{id:'height',label:'Height (px)',type:'number',def:3000},{id:'format',label:'Format',type:'select',opts:[{v:'jpeg',l:'JPEG'},{v:'png',l:'PNG'},{v:'webp',l:'WebP'}],def:'jpeg'}], calc: function(v) { const mp = v.width*v.height/1e6; const size = {jpeg: mp*0.8, png: mp*4, webp: mp*0.4}[v.format]; return { result: '~' + (size<1?size*1024:size).toFixed(0) + (size<1?' KB':' MB'), chart: null, extra: mp.toFixed(1) + ' MP | Rough estimate — varies with detail & compression' }; } },
  { id: 'hosting-cost', name: 'Hosting Cost per Year', desc: 'Compare hosting plans over a year', kw: 'hosting cost per year calculator', inputs: [{id:'monthly',label:'Monthly Price ($)',type:'number',def:10},{id:'setup',label:'Setup Fee ($)',type:'number',def:0},{id:'renewal',label:'Renewal Rate ($/mo)',type:'number',def:15},{id:'years',label:'Years',type:'number',def:3}], calc: function(v) { const firstYear = v.setup + v.monthly*12; const later = v.renewal*12; const total = firstYear + later*(v.years-1); return { result: '$' + total.toFixed(2) + ' total', chart: null, extra: 'Year 1: $' + firstYear.toFixed(2) + ' | Year 2+: $' + later.toFixed(2) + '/yr | ' + (v.renewal>v.monthly?'Watch the renewal hike ⚠':'Stable pricing ✓') }; } },
  { id: 'monitor-refresh', name: 'Refresh Rate vs FPS', desc: 'Find max FPS your monitor can show', kw: 'monitor refresh rate', inputs: [{id:'refresh',label:'Monitor Refresh (Hz)',type:'number',def:144},{id:'fps',label:'Game FPS',type:'number',def:200}], calc: function(v) { const shown = Math.min(v.fps, v.refresh); const wasted = v.fps - v.refresh; const smooth = v.fps >= v.refresh ? 'vsync/GSync helpful' : 'Headroom available'; return { result: 'Visible: ' + shown + ' fps', chart: null, extra: 'Screen: ' + v.refresh + ' Hz | Game: ' + v.fps + ' fps | ' + (wasted>0?'Frames beyond screen: '+wasted+' ('+smooth+')':smooth) + ' | 60Hz=60fps, 144Hz=144fps, 240Hz=240fps' }; } },
  { id: 'streaming-bitrate', name: 'Streaming Bitrate Guide', desc: 'Find ideal streaming bitrate for your setup', kw: 'streaming bitrate guide calculator', inputs: [{id:'resolution',label:'Resolution',type:'select',opts:[{v:'480p',l:'480p'},{v:'720p',l:'720p'},{v:'1080p',l:'1080p'},{v:'1440p',l:'1440p (2K)'},{v:'2160p',l:'4K UHD'}],def:'1080p'},{id:'fps',label:'Frame Rate',type:'select',opts:[{v:'30',l:'30 fps'},{v:'60',l:'60 fps'}],def:'60'}], calc: function(v) { const base = {'480p':1.5,'720p':4.5,'1080p':8,'1440p':16,'2160p':35}; const bitrate = (base[v.resolution]||8) * (v.fps==='60'?1.5:1); const hourGB = bitrate/8*3600/1024; return { result: bitrate.toFixed(1) + ' Mbps upload', chart: null, extra: v.resolution + ' @ ' + v.fps + 'fps | ' + hourGB.toFixed(1) + ' GB/hr | Recommend ' + Math.ceil(bitrate*1.3) + ' Mbps headroom' }; } },
  { id: 'raid-capacity', name: 'RAID Capacity Calculator', desc: 'Calculate usable storage for RAID levels', kw: 'raid capacity calculator', inputs: [{id:'drives',label:'Drive Count',type:'number',def:4},{id:'size',label:'Drive Size (TB)',type:'number',def:4},{id:'level',label:'RAID Level',type:'select',opts:[{v:'0',l:'RAID 0'},{v:'1',l:'RAID 1'},{v:'5',l:'RAID 5'},{v:'6',l:'RAID 6'},{v:'10',l:'RAID 10'}],def:'5'}],    calc: function(v) { const n=v.drives, s=v.size, l=v.level; if (!n || !s) return { result: 'Enter drives & size > 0', chart: null, extra: 'RAID usable capacity depends on level' }; const usable = l==='0' ? n*s : l==='1' ? s : l==='5' ? (n-1)*s : l==='6' ? (n-2)*s : (Math.floor(n/2))*s; const eff = usable/(n*s)*100; const overhead = Math.max(n*s-usable, 0.01); return { result: 'Usable: ' + usable.toFixed(1) + ' TB', chart: Charts.donut([usable, overhead], ['Usable','Parity/Mirror']), extra: 'Raw: ' + (n*s).toFixed(1) + ' TB | Efficiency: ' + eff.toFixed(0) + '% | ' + (l==='0'?'No redundancy':l==='1'?'Mirror':l==='5'?'1-drive parity':l==='6'?'2-drive parity':'Mirrored pairs') + (usable <= 0 ? ' — insufficient drives' : '') }; } },
  { id: 'uptime-calculator', name: 'Uptime / Downtime Calculator', desc: 'Convert uptime % to downtime per year', kw: 'uptime / downtime calculator', inputs: [{id:'uptime',label:'Uptime (%)',type:'number',def:99.9}], calc: function(v) { const pct = Math.min(v.uptime, 100); const minYear = (1-pct/100)*365*24*60; const hrs = minYear/60; return { result: 'Downtime: ' + (hrs < 1 ? Math.round(minYear) + ' min/year' : hrs.toFixed(1) + ' hrs/year'), chart: Charts.donut([pct, 100-pct], ['Uptime','Downtime']), extra: '≈ ' + (minYear/60/24).toFixed(2) + ' days/yr | ' + (minYear/525600*100).toFixed(3) + '% loss | ' + (hrs*24).toFixed(0) + ' min/month' }; } },
  { id: 'device-charge-time', name: 'Device Charging Time', desc: 'Estimate how long your device takes to charge', kw: 'device charging time calculator', inputs: [{id:'batteryMah',label:'Battery (mAh)',type:'number',def:5000},{id:'chargeA',label:'Charger Output (A)',type:'number',def:2},{id:'efficiency',label:'Efficiency (%)',type:'number',def:85}], calc: function(v) { const hours = v.batteryMah / (v.chargeA*1000) / (v.efficiency/100); return { result: hours.toFixed(1) + ' hours', chart: Charts.gauge(hours, 5), extra: v.batteryMah + ' mAh @ ' + v.chargeA + 'A | ' + v.efficiency + '% eff | ' + (hours*60).toFixed(0) + ' minutes' }; } },
  { id: 'data-transfer-cost', name: 'Cloud Data Transfer Cost', desc: 'Estimate cloud egress bandwidth cost', kw: 'cloud egress cost', inputs: [{id:'dataGB',label:'Data Transferred (GB/mo)',type:'number',def:100},{id:'provider',label:'Provider',type:'select',opts:[{v:'aws',l:'AWS'},{v:'azure',l:'Azure'},{v:'gcp',l:'GCP'},{v:'cloudflare',l:'Cloudflare'}],def:'aws'}], calc: function(v) { const rate = {aws:0.09,azure:0.087,gcp:0.12,cloudflare:0}; const cost = v.dataGB * rate[v.provider]; return { result: 'Cost: $' + cost.toFixed(2) + '/month', chart: null, extra: 'Rate: $' + rate[v.provider].toFixed(3) + '/GB | ' + v.dataGB + ' GB/mo' + (v.provider==='cloudflare'?' | Free tier first 1TB ✓':'') }; } },
  { id: 'token-cost', name: 'AI Token Cost Estimator', desc: 'Estimate LLM API costs for your usage', kw: 'ai token cost estimator calculator', inputs: [{id:'inputK',label:'Input Tokens (K/mo)',type:'number',def:500},{id:'outputK',label:'Output Tokens (K/mo)',type:'number',def:100},{id:'model',label:'Model',type:'select',opts:[{v:'gpt4o',l:'GPT-4o'},{v:'gpt4omini',l:'GPT-4o mini'},{v:'claude',l:'Claude Sonnet'},{v:'llama',l:'Llama 3.1 70B'},{v:'grok',l:'Grok-Beta'}],def:'gpt4o'}], calc: function(v) { const price = {gpt4o:{i:2.5,o:10},gpt4omini:{i:0.15,o:0.6},claude:{i:3,o:15},llama:{i:0.88,o:0.88},grok:{i:5,o:15}}; const p = price[v.model]||price.gpt4o; const cost = v.inputK/1000*p.i + v.outputK/1000*p.o; return { result: '$' + cost.toFixed(2) + '/month', chart: Charts.donut([v.inputK/1000*p.i, v.outputK/1000*p.o], ['Input','Output']), extra: 'In: $' + (v.inputK/1000*p.i).toFixed(2) + ' | Out: $' + (v.outputK/1000*p.o).toFixed(2) + ' | $' + p.i + '/M in, $' + p.o + '/M out' }; } },
  // ===== Developer tools pack (Gap 5 — MiniWebTool-style traffic engine) =====
  { id: 'base64-encode', name: 'Base64 Encoder', desc: 'Encode text to Base64 instantly', kw: 'base64 encoder calculator', inputs: [{id:'text',label:'Text to Encode',type:'textarea',def:'Hello CalcPro!'}], calc: function(v) { const out = btoa(unescape(encodeURIComponent(v.text || ''))); return { result: out, chart: null, extra: v.text.length + ' chars → ' + out.length + ' chars base64' }; },
    steps: function(v) { const t = v.text || ''; return ['Input (' + t.length + ' chars): "' + t.slice(0, 40) + (t.length > 40 ? '…' : '') + '"', 'Convert the text to UTF-8 bytes', 'Group the bytes into 24-bit (3-byte) blocks', 'Split each block into four 6-bit values', 'Map each 6-bit value to the Base64 alphabet (A–Z, a–z, 0–9, +, /)']; } },
  { id: 'base64-decode', name: 'Base64 Decoder', desc: 'Decode Base64 back to readable text', kw: 'base64 to text', inputs: [{id:'text',label:'Base64 Text',type:'textarea',def:'SGVsbG8gQ2FsY1BybyE='}], calc: function(v) { try { const out = decodeURIComponent(escape(atob((v.text||'').trim()))); return { result: out, chart: null, extra: 'Decoded successfully ✓' }; } catch(e) { return { result: 'Invalid Base64', chart: null, extra: 'Check the input — it must be valid Base64 text.' }; } },
    steps: function(v) { const t = (v.text || '').trim(); return ['Input Base64: "' + t.slice(0, 40) + (t.length > 40 ? '…' : '') + '"', 'Validate each character against the Base64 alphabet', 'Map each character back to its 6-bit value', 'Reassemble the 6-bit groups into 8-bit bytes', 'Decode the UTF-8 bytes back to readable text']; } },
  { id: 'url-encoder', name: 'URL Encoder / Decoder', desc: 'Encode or decode URL parameters safely', kw: 'url encoder / decoder calculator', inputs: [{id:'text',label:'Text',type:'textarea',def:'https://example.com/?q=hello world&lang=en'},{id:'mode',label:'Mode',type:'select',opts:[{v:'enc',l:'Encode'},{v:'dec',l:'Decode'}],def:'enc'}], calc: function(v) { try { const out = v.mode === 'dec' ? decodeURIComponent(v.text || '') : encodeURIComponent(v.text || ''); return { result: out, chart: null, extra: v.mode === 'enc' ? 'Percent-encoded for URLs' : 'Decoded from percent-encoding' }; } catch(e) { return { result: 'Invalid input', chart: null, extra: 'Could not ' + v.mode + 'ode — check for malformed sequences.' }; } },
    steps: function(v) { const mode = v.mode === 'enc' ? 'Encode' : 'Decode'; return ['Mode: ' + mode, mode === 'Encode' ? 'Percent-encode every unsafe character (space → %20, & → %26, = → %3D)' : 'Replace each %XX escape with its decoded character', 'Letters, digits and - _ . ~ pass through unchanged', 'The result is safe to embed in a URL query string']; } },
  { id: 'json-formatter', name: 'JSON Formatter', desc: 'Pretty-print, minify and validate JSON', kw: 'pretty print json', inputs: [{id:'text',label:'JSON',type:'textarea',def:'{"name":"CalcPro","tools":548}'},{id:'indent',label:'Indent',type:'select',opts:[{v:'2',l:'2 spaces'},{v:'4',l:'4 spaces'},{v:'tab',l:'Tab'}],def:'2'}], calc: function(v) { try { const parsed = JSON.parse(v.text || 'null'); const sp = v.indent === 'tab' ? '\t' : parseInt(v.indent); const out = JSON.stringify(parsed, null, sp); return { result: out, chart: null, extra: '✓ Valid JSON | ' + out.split('\n').length + ' lines' }; } catch(e) { return { result: '⚠ Invalid JSON', chart: null, extra: e.message }; } } },
  { id: 'text-counter', name: 'Word & Character Counter', desc: 'Count words, characters, sentences and reading time', kw: 'word & character counter calculator', inputs: [{id:'text',label:'Text',type:'textarea',def:'Write or paste your text here to count words and characters.'}], calc: function(v) { const t = v.text || ''; const words = t.trim() ? t.trim().split(/\s+/).length : 0; const chars = t.length; const charsNoSp = t.replace(/\s/g,'').length; const sentences = t.split(/[.!?]+/).filter(s=>s.trim()).length; const readMin = Math.ceil(words/200); return { result: words + ' words', chart: Charts.donut([charsNoSp, chars-charsNoSp], ['Letters/Digits','Spaces']), extra: chars + ' characters (' + charsNoSp + ' no spaces) | ' + sentences + ' sentences | ~' + readMin + ' min read' }; },
    steps: function(v) { const t = v.text || ''; const words = t.trim() ? t.trim().split(/\s+/).length : 0; const chars = t.length; const charsNoSp = t.replace(/\s/g,'').length; const sentences = t.split(/[.!?]+/).filter(s=>s.trim()).length; return ['Word count: split trimmed text on whitespace → ' + words + ' word(s)', 'Characters: ' + chars + ' total, ' + charsNoSp + ' without spaces', 'Sentences: ' + sentences + ' (split on . ! ?)', 'Reading time: ' + Math.ceil(words/200) + ' min at 200 words/min']; } },
  { id: 'case-converter', name: 'Text Case Converter', desc: 'Convert text between uppercase, lowercase, title and sentence case', kw: 'text case converter calculator', inputs: [{id:'text',label:'Text',type:'textarea',def:'the quick brown fox jumps over the lazy dog'},{id:'mode',label:'Convert To',type:'select',opts:[{v:'upper',l:'UPPERCASE'},{v:'lower',l:'lowercase'},{v:'title',l:'Title Case'},{v:'sentence',l:'Sentence case'},{v:'camel',l:'camelCase'}],def:'title'}], calc: function(v) { const t = v.text || ''; let out = t;
    if (v.mode==='upper') out = t.toUpperCase();
    else if (v.mode==='lower') out = t.toLowerCase();
    else if (v.mode==='title') out = t.replace(/\w\S*/g, w=>w.charAt(0).toUpperCase()+w.substr(1).toLowerCase());
    else if (v.mode==='sentence') out = t.replace(/(^|[.!?]\s+)([a-z])/g, (m,a,b)=>a+b.toUpperCase());
    else if (v.mode==='camel') out = t.toLowerCase().replace(/[^a-zA-Z0-9]+(.)/g, (m,c)=>c.toUpperCase());
    return { result: out, chart: null, extra: v.mode + ' conversion | ' + t.length + ' → ' + out.length + ' chars' }; } },
  { id: 'hash-generator', name: 'Hash Generator (MD5/SHA)', desc: 'Generate MD5, SHA-1, SHA-256 and SHA-512 hashes', kw: 'hash generator calculator', inputs: [{id:'text',label:'Text',type:'textarea',def:'CalcProMaster'},{id:'algo',label:'Algorithm',type:'select',opts:[{v:'md5',l:'MD5'},{v:'sha1',l:'SHA-1'},{v:'sha256',l:'SHA-256'},{v:'sha512',l:'SHA-512'}],def:'sha256'}], calc: function(v) { const t = v.text || '';
    // Verified RFC 1321 MD5 (classic Joseph Myers implementation — passes standard
    // test vectors for '', 'a', 'abc', 'message digest'). Dependency-free, works offline.
    function md5Hex(input) {
      function RotateLeft(lValue, iShiftBits) { return (lValue << iShiftBits) | (lValue >>> (32 - iShiftBits)); }
      function AddUnsigned(lX, lY) {
        var lX4, lY4, lX8, lY8, lResult;
        lX8 = (lX & 0x80000000); lY8 = (lY & 0x80000000);
        lX4 = (lX & 0x40000000); lY4 = (lY & 0x40000000);
        lResult = (lX & 0x3FFFFFFF) + (lY & 0x3FFFFFFF);
        if (lX4 & lY4) return (lResult ^ 0x80000000 ^ lX8 ^ lY8);
        if (lX4 | lY4) {
          if (lResult & 0x40000000) return (lResult ^ 0xC0000000 ^ lX8 ^ lY8);
          return (lResult ^ 0x40000000 ^ lX8 ^ lY8);
        }
        return (lResult ^ lX8 ^ lY8);
      }
      function F(x, y, z) { return (x & y) | ((~x) & z); }
      function G(x, y, z) { return (x & z) | (y & (~z)); }
      function H(x, y, z) { return (x ^ y ^ z); }
      function I(x, y, z) { return (y ^ (x | (~z))); }
      function FF(a, b, c, d, x, s, ac) { a = AddUnsigned(a, AddUnsigned(AddUnsigned(F(b, c, d), x), ac)); return AddUnsigned(RotateLeft(a, s), b); }
      function GG(a, b, c, d, x, s, ac) { a = AddUnsigned(a, AddUnsigned(AddUnsigned(G(b, c, d), x), ac)); return AddUnsigned(RotateLeft(a, s), b); }
      function HH(a, b, c, d, x, s, ac) { a = AddUnsigned(a, AddUnsigned(AddUnsigned(H(b, c, d), x), ac)); return AddUnsigned(RotateLeft(a, s), b); }
      function II(a, b, c, d, x, s, ac) { a = AddUnsigned(a, AddUnsigned(AddUnsigned(I(b, c, d), x), ac)); return AddUnsigned(RotateLeft(a, s), b); }
      function ConvertToWordArray(string) {
        var lWordCount, lMessageLength = string.length, lNumberOfWords_temp1 = lMessageLength + 8;
        var lNumberOfWords_temp2 = (lNumberOfWords_temp1 - (lNumberOfWords_temp1 % 64)) / 64;
        var lNumberOfWords = (lNumberOfWords_temp2 + 1) * 16;
        var lWordArray = Array(lNumberOfWords - 1), lBytePosition = 0, lByteCount = 0;
        while (lByteCount < lMessageLength) {
          lWordCount = (lByteCount - (lByteCount % 4)) / 4;
          lBytePosition = (lByteCount % 4) * 8;
          lWordArray[lWordCount] = (lWordArray[lWordCount] | (string.charCodeAt(lByteCount) << lBytePosition));
          lByteCount++;
        }
        lWordCount = (lByteCount - (lByteCount % 4)) / 4;
        lBytePosition = (lByteCount % 4) * 8;
        lWordArray[lWordCount] = lWordArray[lWordCount] | (0x80 << lBytePosition);
        lWordArray[lNumberOfWords - 2] = lMessageLength << 3;
        lWordArray[lNumberOfWords - 1] = (lMessageLength >>> 29);
        return lWordArray;
      }
      function WordToHex(lValue) {
        var w = "", t, lByte, lCount;
        for (lCount = 0; lCount <= 3; lCount++) {
          lByte = (lValue >>> (lCount * 8)) & 255;
          t = "0" + lByte.toString(16);
          w = w + t.substr(t.length - 2, 2);
        }
        return w;
      }
      var x = ConvertToWordArray(unescape(encodeURIComponent(input)));
      var a = 0x67452301, b = 0xEFCDAB89, c = 0x98BADCFE, d = 0x10325476;
      var S11 = 7, S12 = 12, S13 = 17, S14 = 22, S21 = 5, S22 = 9, S23 = 14, S24 = 20, S31 = 4, S32 = 11, S33 = 16, S34 = 23, S41 = 6, S42 = 10, S43 = 15, S44 = 21;
      for (var k = 0; k < x.length; k += 16) {
        var AA = a, BB = b, CC = c, DD = d;
        a = FF(a, b, c, d, x[k + 0], S11, -680876936); d = FF(d, a, b, c, x[k + 1], S12, -389564586); c = FF(c, d, a, b, x[k + 2], S13, 606105819); b = FF(b, c, d, a, x[k + 3], S14, -1044525330);
        a = FF(a, b, c, d, x[k + 4], S11, -176418897); d = FF(d, a, b, c, x[k + 5], S12, 1200080426); c = FF(c, d, a, b, x[k + 6], S13, -1473231341); b = FF(b, c, d, a, x[k + 7], S14, -45705983);
        a = FF(a, b, c, d, x[k + 8], S11, 1770035416); d = FF(d, a, b, c, x[k + 9], S12, -1958414417); c = FF(c, d, a, b, x[k + 10], S13, -42063); b = FF(b, c, d, a, x[k + 11], S14, -1990404162);
        a = FF(a, b, c, d, x[k + 12], S11, 1804603682); d = FF(d, a, b, c, x[k + 13], S12, -40341101); c = FF(c, d, a, b, x[k + 14], S13, -1502002290); b = FF(b, c, d, a, x[k + 15], S14, 1236535329);
        a = GG(a, b, c, d, x[k + 1], S21, -165796510); d = GG(d, a, b, c, x[k + 6], S22, -1069501632); c = GG(c, d, a, b, x[k + 11], S23, 643717713); b = GG(b, c, d, a, x[k + 0], S24, -373897302);
        a = GG(a, b, c, d, x[k + 5], S21, -701558691); d = GG(d, a, b, c, x[k + 10], S22, 38016083); c = GG(c, d, a, b, x[k + 15], S23, -660478335); b = GG(b, c, d, a, x[k + 4], S24, -405537848);
        a = GG(a, b, c, d, x[k + 9], S21, 568446438); d = GG(d, a, b, c, x[k + 14], S22, -1019803690); c = GG(c, d, a, b, x[k + 3], S23, -187363961); b = GG(b, c, d, a, x[k + 8], S24, 1163531501);
        a = GG(a, b, c, d, x[k + 13], S21, -1444681467); d = GG(d, a, b, c, x[k + 2], S22, -51403784); c = GG(c, d, a, b, x[k + 7], S23, 1735328473); b = GG(b, c, d, a, x[k + 12], S24, -1926607734);
        a = HH(a, b, c, d, x[k + 5], S31, -378558); d = HH(d, a, b, c, x[k + 8], S32, -2022574463); c = HH(c, d, a, b, x[k + 11], S33, 1839030562); b = HH(b, c, d, a, x[k + 14], S34, -35309556);
        a = HH(a, b, c, d, x[k + 1], S31, -1530992060); d = HH(d, a, b, c, x[k + 4], S32, 1272893353); c = HH(c, d, a, b, x[k + 7], S33, -155497632); b = HH(b, c, d, a, x[k + 10], S34, -1094730640);
        a = HH(a, b, c, d, x[k + 13], S31, 681279174); d = HH(d, a, b, c, x[k + 0], S32, -358537222); c = HH(c, d, a, b, x[k + 3], S33, -722521979); b = HH(b, c, d, a, x[k + 6], S34, 76029189);
        a = HH(a, b, c, d, x[k + 9], S31, -640364487); d = HH(d, a, b, c, x[k + 12], S32, -421815835); c = HH(c, d, a, b, x[k + 15], S33, 530742520); b = HH(b, c, d, a, x[k + 2], S34, -995338651);
        a = II(a, b, c, d, x[k + 0], S41, -198630844); d = II(d, a, b, c, x[k + 7], S42, 1126891415); c = II(c, d, a, b, x[k + 14], S43, -1416354905); b = II(b, c, d, a, x[k + 5], S44, -57434055);
        a = II(a, b, c, d, x[k + 12], S41, 1700485571); d = II(d, a, b, c, x[k + 3], S42, -1894986606); c = II(c, d, a, b, x[k + 10], S43, -1051523); b = II(b, c, d, a, x[k + 1], S44, -2054922799);
        a = II(a, b, c, d, x[k + 8], S41, 1873313359); d = II(d, a, b, c, x[k + 15], S42, -30611744); c = II(c, d, a, b, x[k + 6], S43, -1560198380); b = II(b, c, d, a, x[k + 13], S44, 1309151649);
        a = II(a, b, c, d, x[k + 4], S41, -145523070); d = II(d, a, b, c, x[k + 11], S42, -1120210379); c = II(c, d, a, b, x[k + 2], S43, 718787259); b = II(b, c, d, a, x[k + 9], S44, -343485551);
        a = AddUnsigned(a, AA); b = AddUnsigned(b, BB); c = AddUnsigned(c, CC); d = AddUnsigned(d, DD);
      }
      return (WordToHex(a) + WordToHex(b) + WordToHex(c) + WordToHex(d)).toLowerCase();
    }
    if (v.algo==='md5') return { result: md5Hex(t), chart: null, extra: 'MD5 (128-bit) — ' + t.length + ' chars input' };
    if (typeof crypto !== 'undefined' && crypto.subtle) {
      const algo = {sha1:'SHA-1',sha256:'SHA-256',sha512:'SHA-512'}[v.algo] || 'SHA-256';
      return crypto.subtle.digest(algo, new TextEncoder().encode(t)).then(buf => {
        const hex = [...new Uint8Array(buf)].map(b=>b.toString(16).padStart(2,'0')).join('');
        return { result: hex, chart: null, extra: algo + ' — ' + t.length + ' chars input' };
      });
    }
    return { result: 'crypto.subtle unavailable', chart: null, extra: 'Open over HTTPS or localhost for SHA-1/256/512. MD5 works everywhere.' }; } },
  { id: 'rgb-hex', name: 'RGB ↔ Hex Converter', desc: 'Convert between RGB and HEX color codes', kw: 'rgb to hex, hex to rgb', inputs: [{id:'mode',label:'Convert',type:'select',opts:[{v:'rgb2hex',l:'RGB → HEX'},{v:'hex2rgb',l:'HEX → RGB'}],def:'rgb2hex'},{id:'r',label:'Red (0-255)',type:'number',def:79},{id:'g',label:'Green (0-255)',type:'number',def:70},{id:'b',label:'Blue (0-255)',type:'number',def:229},{id:'hex',label:'HEX (#RRGGBB)',type:'text',def:'#4f46e5'}], calc: function(v) { if (v.mode==='hex2rgb') { const h=(v.hex||'').replace('#',''); if(!/^[0-9a-fA-F]{6}$/.test(h)) return { result:'Invalid HEX', chart:null, extra:'Use #RRGGBB format, e.g. #4f46e5' }; const r=parseInt(h.slice(0,2),16),g=parseInt(h.slice(2,4),16),b=parseInt(h.slice(4,6),16); return { result:'rgb('+r+', '+g+', '+b+')', chart:null, extra:'#'+h.toUpperCase()+' → rgb | CSS: rgb('+r+', '+g+', '+b+')' }; } const clamp=(n)=>Math.max(0,Math.min(255,Math.round(n))); const r=clamp(v.r),g=clamp(v.g),b=clamp(v.b); const hex='#'+[r,g,b].map(x=>x.toString(16).padStart(2,'0')).join('').toUpperCase(); return { result: hex, chart:null, extra:'rgb('+r+', '+g+', '+b+') → '+hex+' | Use in CSS: '+hex }; } },
  { id: 'uuid-generator', name: 'UUID v4 Generator', desc: 'Generate one or many random UUID v4 identifiers', kw: 'uuid v4 generator calculator', inputs: [{id:'count',label:'How Many',type:'number',def:1,slider:{min:1,max:50,step:1}}], calc: function(v) { const n=Math.max(1,Math.min(50,Math.round(v.count||1))); const secureRandom=function(){ try { if (typeof crypto!=='undefined' && crypto.getRandomValues) { const a=new Uint32Array(1); crypto.getRandomValues(a); return a[0]>>>0; } } catch(e){} const s=Math.floor(Math.random()*4294967296); return s>>>0; }; const out=[]; for(let i=0;i<n;i++){ const hex=[]; for(let j=0;j<16;j++){ hex.push(secureRandom()&0xff); } hex[6]=(hex[6]&0x0f)|0x40; hex[8]=(hex[8]&0x3f)|0x80; const h=function(b){ return (b<16?'0':'')+b.toString(16); }; out.push(h(hex[0])+h(hex[1])+h(hex[2])+h(hex[3])+'-'+h(hex[4])+h(hex[5])+'-'+h(hex[6])+h(hex[7])+'-'+h(hex[8])+h(hex[9])+'-'+h(hex[10])+h(hex[11])+h(hex[12])+h(hex[13])+h(hex[14])+h(hex[15])); } return { result: out.join('\n'), chart: null, extra: n + ' UUID v4' + (n>1?'s':'') + ' — 122 bits of randomness, collision-safe' }; } },
  { id: 'password-generator', name: 'Strong Password Generator', desc: 'Generate secure random passwords with adjustable length and character sets', kw: 'strong password generator calculator', inputs: [{id:'length',label:'Length',type:'number',def:16,slider:{min:8,max:64,step:1}},{id:'upper',label:'A-Z',type:'checkbox',def:true},{id:'lower',label:'a-z',type:'checkbox',def:true},{id:'digits',label:'0-9',type:'checkbox',def:true},{id:'symbols',label:'!@#',type:'checkbox',def:true},{id:'count',label:'How Many',type:'number',def:1}], calc: function(v) { let pool=''; if(v.upper)pool+='ABCDEFGHIJKLMNOPQRSTUVWXYZ'; if(v.lower)pool+='abcdefghijklmnopqrstuvwxyz'; if(v.digits)pool+='0123456789'; if(v.symbols)pool+='!@#$%^&*()-_=+[]{};:,.?'; if(!pool)pool='abcdefghijklmnopqrstuvwxyz0123456789'; const len=Math.max(8,Math.min(64,Math.round(v.length||16))); const n=Math.max(1,Math.min(10,Math.round(v.count||1)));
    // Secure uniform index in [0, n) via crypto.getRandomValues with rejection sampling
    // (modulo bias removed). Falls back to Math.random only when crypto is unavailable.
    const rnd=function(n){ const limit=Math.floor(4294967296/n)*n; let x=0; do { try { if (typeof crypto!=='undefined' && crypto.getRandomValues) { const a=new Uint32Array(1); crypto.getRandomValues(a); x=a[0]; } else { x=Math.floor(Math.random()*4294967296); } } catch(e){ x=Math.floor(Math.random()*4294967296); } } while (x>=limit); return x%n; };
    const out=[]; for(let k=0;k<n;k++){ let p=''; for(let i=0;i<len;i++) p+=pool[rnd(pool.length)]; out.push(p); }
    const entropy=Math.round(len*Math.log2(pool.length));
    return { result: out.join('\n'), chart: null, extra: len + ' chars × ' + pool.length + '-char pool | ' + entropy + ' bits entropy' + (entropy>=100?' — Very strong ✓':'') }; } },
  { id: 'ip-subnet', name: 'IP Subnet Calculator', desc: 'Calculate network address, broadcast, range and usable hosts for IPv4 CIDR', kw: 'ip subnet calculator', inputs: [{id:'ip',label:'IP Address',type:'text',def:'192.168.1.10'},{id:'prefix',label:'Prefix Length (/24)',type:'number',def:24}], calc: function(v) { const parts=(v.ip||'').split('.').map(Number); if(parts.length!==4||parts.some(x=>isNaN(x)||x<0||x>255)) return { result:'Invalid IP', chart:null, extra:'Use dotted-decimal IPv4, e.g. 192.168.1.10' };
    const p=Math.max(0,Math.min(32,Math.round(v.prefix||24))); const mask=p===0?0:(0xffffffff<<(32-p))>>>0;
    const ipInt=((parts[0]<<24)|(parts[1]<<16)|(parts[2]<<8)|parts[3])>>>0; const net=ipInt&mask; const bcast=net|(~mask>>>0);
    const hosts=32-p<=1?0:(1<<(32-p))-2; const fmt=(n)=>[(n>>>24)&255,(n>>>16)&255,(n>>>8)&255,n&255].join('.');
    const maskStr=fmt(mask);
    return { result: 'Network: ' + fmt(net) + '/' + p, chart: Charts.donut([Math.max(hosts,0), (1<<(32-p))-hosts-2], ['Usable hosts','Network+Broadcast']), extra: 'Netmask: ' + maskStr + ' | Broadcast: ' + fmt(bcast) + ' | Usable hosts: ' + Math.max(hosts,0) + ' | CIDR: /' + p }; } },
  { id: 'cron-validate', name: 'Cron Expression Validator', desc: 'Validate and explain cron schedule expressions', kw: 'cron expression validator calculator', inputs: [{id:'expr',label:'Cron Expression',type:'text',def:'0 9 * * 1-5'},{id:'detail',label:'Detail',type:'select',opts:[{v:'brief',l:'Brief'},{v:'full',l:'Full description'}],def:'brief'}], calc: function(v) { const e=(v.expr||'').trim().split(/\s+/); if(e.length<5||e.length>6) return { result:'⚠ Invalid', chart:null, extra:'Cron needs 5 fields (min hour dom month dow) or 6 with seconds.' };
    const fieldOk=(f,range)=>f==='*'||f.split(',').every(p=>{ const m=p.match(/^(\*|\d+)(?:-(\d+))?(?:\/(\d+))?$/); if(!m) return false; if(m[1]==='*') return true; const lo=+m[1],hi=m[2]?+m[2]:lo; return lo>=range[0]&&hi<=range[1]&&lo<=hi; });
    const r=[[0,59],[0,59],[0,23],[1,31],[1,12],[0,6]]; const start=e.length===6?0:1; const ok=e.slice(start).every((f,i)=>fieldOk(f,r[i]));
    const DOW=['Sun','Mon','Tue','Wed','Thu','Fri','Sat'];
    let desc=''; if(ok){ const min=e.length===6?e[0]:'0',hrs=e[start],dom=e[start+1],mon=e[start+2],dow=e[start+3]; desc='Runs at '+(hrs==='*'?'every hour':hrs.padStart(2,'0')+':00')+(min!=='0'&&min!=='*'?' (min '+min+')':'')+' on '+(dow==='*'?(dom==='*'?'every day':dom+'th of month'):(dow.split(',').map(d=>DOW[+d]||d).join(', ')))+(mon!=='*'?' in month(s) '+mon:''); }
    return { result: ok ? '✓ Valid' : '⚠ Invalid', chart: null, extra: ok ? desc : 'Fix the expression — see fields: minute hour day-of-month month day-of-week' }; },
    steps: function(v) { const e=(v.expr||'').trim().split(/\s+/); return ['Expression: "' + (v.expr || '') + '"', 'Split into ' + e.length + ' field(s): minute | hour | day-of-month | month | day-of-week' + (e.length === 6 ? ' (+ seconds first)' : ''), 'Range-check each field (min 0–59, hour 0–23, dom 1–31, month 1–12, dow 0–6)', (e.length >= 5 && e.length <= 6) ? 'Field count OK — valid if every field passes its range check' : 'Cron needs 5 or 6 fields → invalid']; } },
  // ===== Reference tables hub (Gap 6 — RapidTables-style instant lookup) =====
  { id: 'resistor-color-code', name: 'Resistor Color Code Calculator', desc: 'Decode 4-band resistor colors to ohms and tolerance instantly', kw: 'resistor color code, 4 band resistor, ohm color code', inputs: [{id:'band1',label:'1st Band',type:'select',opts:[{v:'0',l:'Black'},{v:'1',l:'Brown'},{v:'2',l:'Red'},{v:'3',l:'Orange'},{v:'4',l:'Yellow'},{v:'5',l:'Green'},{v:'6',l:'Blue'},{v:'7',l:'Violet'},{v:'8',l:'Gray'},{v:'9',l:'White'}],def:'4'},{id:'band2',label:'2nd Band',type:'select',opts:[{v:'0',l:'Black'},{v:'1',l:'Brown'},{v:'2',l:'Red'},{v:'3',l:'Orange'},{v:'4',l:'Yellow'},{v:'5',l:'Green'},{v:'6',l:'Blue'},{v:'7',l:'Violet'},{v:'8',l:'Gray'},{v:'9',l:'White'}],def:'7'},{id:'band3',label:'Multiplier',type:'select',opts:[{v:'1',l:'Black ×1'},{v:'10',l:'Brown ×10'},{v:'100',l:'Red ×100'},{v:'1000',l:'Orange ×1k'},{v:'10000',l:'Yellow ×10k'},{v:'100000',l:'Green ×100k'},{v:'1000000',l:'Blue ×1M'},{v:'10000000',l:'Violet ×10M'},{v:'0.1',l:'Gold ×0.1'},{v:'0.01',l:'Silver ×0.01'}],def:'100'},{id:'tol',label:'Tolerance',type:'select',opts:[{v:'1',l:'Brown ±1%'},{v:'2',l:'Red ±2%'},{v:'5',l:'Gold ±5%'},{v:'10',l:'Silver ±10%'}],def:'5'}], calc: function(v) { const digits = v.band1 + v.band2; const base = parseInt(digits); const ohms = base * parseFloat(v.band3);
      const fmt=(o)=>{ if(o>=1e6) return (o/1e6).toFixed(2).replace(/\.?0+$/,'')+' MΩ'; if(o>=1e3) return (o/1e3).toFixed(2).replace(/\.?0+$/,'')+' kΩ'; return o+' Ω'; };
      const lo=ohms*(1-v.tol/100), hi=ohms*(1+v.tol/100);
      return { result: fmt(ohms) + ' ±' + v.tol + '%', chart: null, extra: 'Digits: ' + digits + ' | Range: ' + fmt(lo) + ' – ' + fmt(hi) + ' | Band colors: ' + [v.band1,v.band2].join('-') + ' ×' + v.band3 }; } },
  { id: 'ascii-table', name: 'ASCII Table Reference', desc: 'Look up ASCII character codes (0-127) — decimal, hex, binary', kw: 'ascii code lookup', inputs: [{id:'char',label:'Character',type:'text',def:'A'},{id:'range',label:'Show Range',type:'select',opts:[{v:'all',l:'Full table (0-127)'},{v:'print',l:'Printable (32-126)'}],def:'print'}], calc: function(v) {
      const c=(v.char||'').charAt(0); let row='';
      if (c) { const code=c.charCodeAt(0); row='<table class="ref-table"><caption>ASCII for “'+Security.sanitizeHtml(c)+'”</caption><thead><tr><th>Decimal</th><th>Hex</th><th>Binary</th><th>Escape</th></tr></thead><tbody><tr><td>'+code+'</td><td>0x'+code.toString(16).toUpperCase()+'</td><td>'+code.toString(2).padStart(8,'0')+'</td><td>'+(code<32?'Ctrl-'+String.fromCharCode(64+code):(code===127?'DEL':'—'))+'</td></tr></tbody></table>'; }
      let table=''; if (v.range==='print') {
        table='<table class="ref-table"><thead><tr><th>Dec</th><th>Hex</th><th>Char</th><th>Dec</th><th>Hex</th><th>Char</th></tr></thead><tbody>';
        let r=''; for(let i=32;i<=126;i+=2){ r+='<tr><td>'+i+'</td><td>0x'+i.toString(16)+'</td><td>'+Security.sanitizeHtml(String.fromCharCode(i))+'</td><td>'+(i+1)+'</td><td>0x'+(i+1).toString(16)+'</td><td>'+Security.sanitizeHtml(String.fromCharCode(i+1))+'</td></tr>'; } table+=r+'</tbody></table>';
      }
      return { result: 'ASCII reference ready', isHtml: true, chart: null, extraHtml: true, extra: 'Enter any character to see its code — or browse the printable table.' + row + table }; },
    steps: function(v) { const c = (v.char || '').charAt(0); if (!c) return ['Enter a character to look up its code']; const code = c.charCodeAt(0); return ['Character: "' + c + '"', 'Decimal code: ' + code, 'Hexadecimal: 0x' + code.toString(16).toUpperCase(), 'Binary: ' + code.toString(2).padStart(8, '0'), (code < 32 ? 'Control character (Ctrl-' + String.fromCharCode(64 + code) + ')' : code === 127 ? 'Delete (DEL)' : 'Printable ASCII character')]; } },
  { id: 'awg-reference', name: 'Wire Gauge (AWG) Reference', desc: 'Look up AWG wire diameter, area and ampacity (RapidTables-style reference)', kw: 'american wire gauge', inputs: [{id:'awg',label:'AWG Size',type:'number',def:14,slider:{min:0,max:40,step:1}}], calc: function(v) { const awg=Math.max(0,Math.min(40,Math.round(v.awg||14)));
      const d=0.127*Math.pow(92,(36-awg)/39); const area=Math.PI*Math.pow(d/2,2);
      const amp={0:85,2:95,4:70,6:55,8:40,10:30,12:20,14:15,16:10,18:7,20:5,22:3,24:2}[awg] || (awg<8?80-awg*5:Math.max(1,Math.round(60*Math.pow(0.89,awg-8))));
      return { result: awg+' AWG — '+d.toFixed(3)+' mm diameter', chart: null, extra: 'Area: '+area.toFixed(3)+' mm² ('+(area*1973.5).toFixed(1)+' CM) | Ampacity (chassis wiring): ~'+amp+' A | '+d.toFixed(4)+' in diameter' }; } },


  // ═══ NEW: 13 additional calculators ═══

  { id: 'aspect-ratio', name: 'Aspect Ratio Calculator', desc: 'Find dimensions for an aspect ratio', kw: 'aspect ratio calculator 16:9 4:3 resize',
    inputs: [{id:'ratioW',label:'Ratio Width (e.g. 16)',type:'number',def:16},{id:'ratioH',label:'Ratio Height (e.g. 9)',type:'number',def:9},{id:'width',label:'Known Width (px)',type:'number',def:1920}],
    calc: function(v){var height=v.width*v.ratioH/v.ratioW;var ratio=(v.ratioW/v.ratioH).toFixed(3);return{result:Math.round(height)+' px height',chart:Charts.bar([v.width,height],['Width','Height']),extra:'Aspect '+v.ratioW+':'+v.ratioH+' ('+ratio+')'};},
    steps: function(v){var height=v.width*v.ratioH/v.ratioW;return['Height = width × ratioH / ratioW','= '+v.width+' × '+v.ratioH+' / '+v.ratioW,'= '+Math.round(height)+' px'];} },
  { id: 'color-contrast', name: 'Color Contrast Checker', desc: 'WCAG contrast ratio between two colors', kw: 'color contrast checker wcag ratio accessibility',
    inputs: [{id:'fg',label:'Foreground (hex)',type:'text',def:'#333333'},{id:'bg',label:'Background (hex)',type:'text',def:'#ffffff'}],
    calc: function(v){function lum(hex){var c=hex.replace('#','');if(c.length===3)c=c.split('').map(function(x){return x+x;}).join('');var rgb=[0,2,4].map(function(i){var n=parseInt(c.substr(i,2),16)/255;return n<=0.03928?n/12.92:Math.pow((n+0.055)/1.055,2.4);});return 0.2126*rgb[0]+0.7152*rgb[1]+0.0722*rgb[2];}var l1=lum(v.fg),l2=lum(v.bg);var hi=Math.max(l1,l2),lo=Math.min(l1,l2);var ratio=(hi+0.05)/(lo+0.05);var wcag=ratio>=4.5?'AA ✓ (normal text)':ratio>=3?'AA (large text only)':'FAIL';return{result:ratio.toFixed(2)+':1',chart:Charts.gauge(Math.min(ratio,21),21),extra:wcag};},
    steps: function(v){return['Compute relative luminance of each color','Contrast = (L1+0.05) / (L2+0.05)','WCAG: 4.5:1 for normal text, 3:1 for large text'];} },
  { id: 'read-time', name: 'Reading Time (Web)', desc: 'Reading time for a webpage or article', kw: 'reading time calculator words web article minutes',
    inputs: [{id:'words',label:'Word Count',type:'number',def:1200},{id:'wpm',label:'Words per Minute',type:'number',def:220}],
    calc: function(v){var min=v.words/v.wpm;var secs=Math.round((min-Math.floor(min))*60);return{result:Math.floor(min)+' min '+(secs?'read ('+secs+' s)':'read'),chart:Charts.bar([min],['Minutes']),extra:'Average reader: 200–250 wpm'};},
    steps: function(v){var min=v.words/v.wpm;return['Time = '+v.words+' words ÷ '+v.wpm+' wpm','= '+min.toFixed(1)+' minutes'];} },
  { id: 'font-size-conv', name: 'PX to REM/EM Converter', desc: 'Convert px to rem and em', kw: 'px to rem converter font size em root',
    inputs: [{id:'px',label:'Pixels (px)',type:'number',def:16},{id:'root',label:'Root Font Size (px)',type:'number',def:16}],
    calc: function(v){var rem=v.px/v.root;var percent=v.px/v.root*100;return{result:rem.toFixed(3)+' rem',chart:Charts.bar([v.px],['px']),extra:percent.toFixed(1)+'% of root | '+(rem).toFixed(2)+'em'};},
    steps: function(v){var rem=v.px/v.root;return['1 rem = root size = '+v.root+' px','rem = '+v.px+' / '+v.root+' = '+rem.toFixed(3),'% = '+rem.toFixed(3)*100+'%'];} },
  { id: 'storage-need', name: 'Storage Needs Calculator', desc: 'Storage space for your files', kw: 'storage calculator GB TB photos videos estimate',
    inputs: [{id:'photos',label:'Photos (millions)',type:'number',def:10},{id:'videoHours',label:'Video Hours (1080p)',type:'number',def:20},{id:'documents',label:'Documents (GB)',type:'number',def:5},{id:'music',label:'Music Hours',type:'number',def:200}],
    calc: function(v){var gb=v.photos*5+v.videoHours*3+v.documents+v.music*0.1;var tb=gb/1024;var plan=gb<15?'15 GB':gb<110?'100 GB':gb<210?'200 GB':gb<2200?'2 TB':'4 TB+';return{result:gb.toFixed(0)+' GB',chart:Charts.bar([gb],['GB']),extra:'≈ '+tb.toFixed(2)+' TB | A '+plan+' plan covers it'};},
    steps: function(v){var gb=v.photos*5+v.videoHours*3+v.documents+v.music*0.1;return['Photos: '+v.photos+'M × 5 MB = '+(v.photos*5)+' GB','Video: '+v.videoHours+' h × 3 GB/h = '+(v.videoHours*3)+' GB','Documents/music: '+(v.documents+v.music*0.1)+' GB','Total ≈ '+gb.toFixed(0)+' GB ('+(gb/1024).toFixed(2)+' TB)'];} },
  { id: 'latency-bandwidth', name: 'Latency-Bandwidth Calculator', desc: 'Transfer time from bandwidth and latency', kw: 'bandwidth latency calculator transfer time file',
    inputs: [{id:'sizeMB',label:'File Size (MB)',type:'number',def:100},{id:'mbps',label:'Bandwidth (Mbps)',type:'number',def:100},{id:'latencyMs',label:'RTT Latency (ms)',type:'number',def:50}],
    calc: function(v){var transferSec=v.sizeMB*8/v.mbps;var roundTrips=Math.min(20,Math.ceil(v.sizeMB/(v.mbps*v.latencyMs/1000*0.1)));var total=transferSec+roundTrips*v.latencyMs/1000;return{result:total.toFixed(1)+' s',chart:Charts.bar([transferSec,total-transferSec],['Pure transfer','Latency overhead']),extra:'Pure: '+transferSec.toFixed(1)+' s | '+Math.round(v.mbps/8)+' MB/s max'};},
    steps: function(v){var transferSec=v.sizeMB*8/v.mbps;var roundTrips=Math.min(20,Math.ceil(v.sizeMB/(v.mbps*v.latencyMs/1000*0.1)));var total=transferSec+roundTrips*v.latencyMs/1000;return['Pure transfer = '+v.sizeMB+' MB × 8 / '+v.mbps+' Mbps = '+transferSec.toFixed(1)+' s','With '+v.latencyMs+' ms RTT overhead ≈ '+total.toFixed(1)+' s total'];} },
  { id: 'download-time', name: 'Download Time Calculator', desc: 'Time to download a file', kw: 'download time calculator file size speed',
    inputs: [{id:'size',label:'File Size',type:'number',def:500},{id:'unit',label:'Size Unit',type:'select',opts:[{v:'MB',l:'MB'},{v:'GB',l:'GB'}],def:'MB'},{id:'speed',label:'Download Speed (Mbps)',type:'number',def:50}],
    calc: function(v){var mb=v.unit==='GB'?v.size*1024:v.size;var secs=mb*8/v.speed;var hours=Math.floor(secs/3600);var mins=Math.floor((secs%3600)/60);var s=Math.round(secs%60);var label='';if(hours>0)label+=hours+'h ';if(mins>0||hours>0)label+=mins+'m ';if(s>0||label==='')label+=s+'s';return{result:label,chart:Charts.bar([mb],['Size (MB)']),extra:secs.toFixed(0)+' s total at '+(v.speed/8)+' MB/s'};},
    steps: function(v){var mb=v.unit==='GB'?v.size*1024:v.size;var secs=mb*8/v.speed;return['Size = '+mb+' MB','Mbps → MB/s = '+v.speed+'/8 = '+(v.speed/8)+' MB/s','Time = '+mb+' / '+(v.speed/8)+' = '+secs.toFixed(0)+' s = '+(secs/60).toFixed(1)+' min'];} },
  { id: 'audio-file-size', name: 'Audio File Size Calculator', desc: 'File size of an audio recording', kw: 'audio file size calculator bitrate duration wav mp3',
    inputs: [{id:'minutes',label:'Duration (min)',type:'number',def:60},{id:'bitrate',label:'Bitrate (kbps)',type:'number',def:128}],
    calc: function(v){var mb=v.bitrate*v.minutes*60/8/1024;var perHour=v.bitrate*3600/8/1024;return{result:mb.toFixed(0)+' MB',chart:Charts.bar([mb],['MB']),extra:'~'+perHour.toFixed(0)+' MB per hour at '+v.bitrate+' kbps'};},
    steps: function(v){var mb=v.bitrate*v.minutes*60/8/1024;return['Bytes = kbps × 1000 × secs / 8','MB = '+v.bitrate+' × '+v.minutes+' × 60 / 8 / 1024 = '+mb.toFixed(0)];} },
  { id: 'backup-size', name: 'Backup Size Calculator', desc: 'Backup storage over retention', kw: 'backup size calculator incremental retention storage',
    inputs: [{id:'fullGB',label:'Full Backup (GB)',type:'number',def:200},{id:'incrementalGB',label:'Daily Change (GB)',type:'number',def:5},{id:'days',label:'Retention (days)',type:'number',def:30}],
    calc: function(v){var total=v.fullGB+v.incrementalGB*v.days;var withFull=total+v.fullGB;return{result:total.toFixed(0)+' GB',chart:Charts.bar([v.fullGB,v.incrementalGB*v.days],['Full','Incrementals']),extra:withFull.toFixed(0)+' GB with second full | plan for 2× headroom'};},
    steps: function(v){var total=v.fullGB+v.incrementalGB*v.days;return['Incremental total = '+v.incrementalGB+' GB × '+v.days+' = '+(v.incrementalGB*v.days)+' GB','Storage = full + incrementals = '+v.fullGB+' + '+(v.incrementalGB*v.days)+' = '+total.toFixed(0)+' GB','Rule of thumb: size for 2× that'];} },
  { id: 'screen-size', name: 'TV Screen Size Calculator', desc: 'Recommended TV size by distance', kw: 'tv size calculator distance viewing recommended',
    inputs: [{id:'distance',label:'Viewing Distance (ft)',type:'number',def:8},{id:'resolution',label:'Resolution',type:'select',opts:[{v:'4k',l:'4K (UHD)'},{v:'1080p',l:'1080p'}],def:'4k'}],
    calc: function(v){var inches=v.distance*12;var size=Math.round(inches/1.5);var min4k=Math.round(inches/2);return{result:size+'" recommended',chart:Charts.bar([size],['Size (in)']),extra:v.resolution==='4k'?'4K min: ~'+min4k+'" | up to '+(v.distance*12/1).toFixed(0)+'"+ works':'1080p sweet spot: '+size+'"'};},
    steps: function(v){var inches=v.distance*12;var size=Math.round(inches/1.5);return['Viewing distance = '+v.distance+' ft = '+inches+'"','4K rule: distance/1.5 = screen size','Recommended ≈ '+size+'"'];} },
  { id: 'streaming-quality', name: 'Streaming Data Calculator', desc: 'Data used by streaming quality', kw: 'streaming data usage calculator netflix youtube quality',
    inputs: [{id:'hours',label:'Hours per Day',type:'number',def:2},{id:'quality',label:'Quality',type:'select',opts:[{v:'sd',l:'SD (1 GB/h)'},{v:'hd',l:'HD (3 GB/h)'},{v:'4k',l:'4K (7 GB/h)'}],def:'hd'},{id:'days',label:'Days per Month',type:'number',def:30}],
    calc: function(v){var gbPerHr={'sd':1,'hd':3,'4k':7}[v.quality];var daily=gbPerHr*v.hours;var monthly=daily*v.days;return{result:monthly.toFixed(0)+' GB/month',chart:Charts.bar([daily,monthly],['Daily','Monthly']),extra:monthly/1024>1?((monthly/1024).toFixed(1)+' TB/month'):(daily.toFixed(1)+' GB/day')};},
    steps: function(v){var gbPerHr={'sd':1,'hd':3,'4k':7}[v.quality];var daily=gbPerHr*v.hours;var monthly=daily*v.days;return['Quality '+v.quality+' uses ~'+gbPerHr+' GB/hour','Daily = '+gbPerHr+' × '+v.hours+' = '+daily.toFixed(1)+' GB','Monthly = '+daily.toFixed(1)+' × '+v.days+' = '+monthly.toFixed(0)+' GB'];} },
  { id: 'code-line-count', name: 'Code Line Estimator', desc: 'Lines of code for a project', kw: 'lines of code estimator project size features',
    inputs: [{id:'features',label:'Features/Screens',type:'number',def:10},{id:'complexity',label:'Complexity',type:'select',opts:[{v:'simple',l:'Simple (~100 loc)'},{v:'medium',l:'Medium (~400 loc)'},{v:'complex',l:'Complex (~1000 loc)'}],def:'medium'},{id:'devs',label:'Developers',type:'number',def:2},{id:'days',label:'Working Days Available',type:'number',def:30}],
    calc: function(v){var loc=v.features*{simple:100,medium:400,complex:1000}[v.complexity];var perDevPerDay=100;var capacity=v.devs*v.days*perDevPerDay;var feasible=loc<=capacity;return{result:loc.toLocaleString()+' LOC',chart:Charts.bar([loc,capacity],['Needed','Capacity']),extra:feasible?'Feasible with '+v.devs+' devs × '+v.days+' days':'Need '+Math.ceil((loc-capacity)/perDevPerDay/v.devs)+' more dev-days'};},
    steps: function(v){var loc=v.features*{simple:100,medium:400,complex:1000}[v.complexity];var capacity=v.devs*v.days*100;return['Est. LOC = '+v.features+' × '+{simple:100,medium:400,complex:1000}[v.complexity]+' = '+loc.toLocaleString(),'Team capacity ≈ '+v.devs+' × '+v.days+' × 100 = '+capacity.toLocaleString(),loc<=capacity?'On track':'Over capacity by '+(loc-capacity).toLocaleString()+' LOC'];} },

  { id: 'dbm-watts', name: 'dBm ↔ Watts Converter', desc: 'Convert between dBm and watts for RF power', kw: 'dbm watts converter rf power milliwatts decibel',
    inputs: [{id:'mode',label:'Convert',type:'select',opts:[{v:'dbm2w',l:'dBm → Watts'},{v:'w2dbm',l:'Watts → dBm'}],def:'dbm2w'},{id:'value',label:'Value',type:'number',def:30}],
    calc: function(v){ if(v.mode==='dbm2w'){ var w=Math.pow(10,(v.value-30)/10); return{result:w<0.001?Math.round(w*100000)/100+' mW':w.toFixed(4)+' W',chart:'',extra:'P(W) = 10^((dBm−30)/10) | 0 dBm = 1 mW'}; } if(v.value<=0) return{result:'Watts must be positive',chart:'',extra:''}; var dbm=30+10*Math.log10(v.value); return{result:dbm.toFixed(2)+' dBm',chart:'',extra:'dBm = 30 + 10·log10(P(W))'};},
    steps: function(v){ if(v.mode==='dbm2w'){ var w=Math.pow(10,(v.value-30)/10); return['Formula: P(W) = 10^((dBm−30)/10)','Step 1: ('+v.value+'−30)/10 = '+((v.value-30)/10),'Step 2: 10^'+((v.value-30)/10)+' = '+(w<0.001?Math.round(w*100000)/100+' mW':w.toFixed(4)+' W')];} var dbm=30+10*Math.log10(v.value); return['Formula: dBm = 30 + 10·log10(P)','Step 1: 10·log10('+v.value+') = '+(10*Math.log10(v.value)).toFixed(3),'Step 2: dBm = 30 + '+(10*Math.log10(v.value)).toFixed(3)+' = '+dbm.toFixed(2)];} },

  { id: 'api-cost', name: 'API Cost Calculator', desc: 'Monthly API usage cost estimate', kw: 'api cost calculator pricing requests compute',
    inputs: [{id:'requests',label:'Requests per Month',type:'number',def:100000},{id:'price',label:'Price per 1000 Requests ($)',type:'number',def:0.5},{id:'computeHrs',label:'Compute Hours per Month',type:'number',def:50},{id:'computeRate',label:'Compute Rate ($/hr)',type:'number',def:0.15}],
    calc: function(v){ var reqCost=v.requests/1000*v.price; var compCost=v.computeHrs*v.computeRate; return{result:'Total: $'+(reqCost+compCost).toFixed(2)+'/mo',chart:Charts.donut([reqCost,compCost],['Requests','Compute']),extra:'Requests: $'+reqCost.toFixed(2)+' | Compute: $'+compCost.toFixed(2)+' | Per request: $'+((reqCost+compCost)/v.requests).toFixed(5)};},
    steps: function(v){ var reqCost=v.requests/1000*v.price; var compCost=v.computeHrs*v.computeRate; return['Step 1: Request cost = '+v.requests.toLocaleString()+' / 1000 × $'+v.price+' = $'+reqCost.toFixed(2),'Step 2: Compute = '+v.computeHrs+' h × $'+v.computeRate+' = $'+compCost.toFixed(2),'Step 3: Total = $'+(reqCost+compCost).toFixed(2)+' per month'];} },

  { id: 'keyword-density', name: 'Keyword Density Calculator', desc: 'How often a keyword appears in your text', kw: 'keyword density calculator seo percentage',
    inputs: [{id:'text',label:'Text',type:'text',def:'loan calculator loan payment loan rate'},{id:'keyword',label:'Keyword',type:'text',def:'loan'}],
    calc: function(v){ var words=(v.text.match(/[\w\u00C0-\u024F']+/g)||[]).length; var kw=String(v.keyword).toLowerCase().trim(); var occ=(v.text.toLowerCase().match(new RegExp(kw.replace(/[.*+?^${}()|[\]\\]/g,'\\$&'),'g'))||[]).length; var dens=words>0?occ/words*100:0; return{result:'Density: '+dens.toFixed(1)+'%',chart:Charts.gauge(dens,10),extra:occ+' occurrence(s) in '+words+' words | 1–3% is the commonly cited SEO range'};},
    steps: function(v){ var words=(v.text.match(/[\w\u00C0-\u024F']+/g)||[]).length; var occ=(v.text.toLowerCase().match(new RegExp(String(v.keyword).toLowerCase().trim().replace(/[.*+?^${}()|[\]\\]/g,'\\$&'),'g'))||[]).length; var dens=occ/words*100; return['Step 1: Word count = '+words,'Step 2: Occurrences of “'+v.keyword+'” = '+occ,'Step 3: Density = '+occ+' / '+words+' × 100 = '+dens.toFixed(1)+'%'];} },

  { id: 'reading-grade', name: 'Readability Score Calculator', desc: 'Flesch Reading Ease and grade level', kw: 'readability score calculator flesch reading ease grade',
    inputs: [{id:'words',label:'Total Words',type:'number',def:100},{id:'sentences',label:'Total Sentences',type:'number',def:8},{id:'syllables',label:'Total Syllables',type:'number',def:140}],
    calc: function(v){ if(v.words<=0||v.sentences<=0) return{result:'Words and sentences must be > 0',chart:'',extra:''}; var fre=206.835-1.015*(v.words/v.sentences)-84.6*(v.syllables/v.words); var grade=0.39*(v.words/v.sentences)+11.8*(v.syllables/v.words)-15.59; var band=fre>=90?'Very easy':fre>=80?'Easy':fre>=70?'Fairly easy':fre>=60?'Standard':fre>=50?'Fairly difficult':fre>=30?'Difficult':'Very difficult'; return{result:'Flesch score: '+fre.toFixed(1),chart:Charts.gauge(fre,100),extra:band+' | Grade level ≈ '+grade.toFixed(1)+' | 60–70 is plain English'};},
    steps: function(v){ var fre=206.835-1.015*(v.words/v.sentences)-84.6*(v.syllables/v.words); var grade=0.39*(v.words/v.sentences)+11.8*(v.syllables/v.words)-15.59; return['Formula: 206.835 − 1.015×(W/S) − 84.6×(Syl/W)','Step 1: W/S = '+v.words+'/'+v.sentences+' = '+(v.words/v.sentences).toFixed(2),'Step 2: Syl/W = '+v.syllables+'/'+v.words+' = '+(v.syllables/v.words).toFixed(2),'Step 3: Score = '+fre.toFixed(1)+' | Grade ≈ '+grade.toFixed(1)];} },

  { id: 'typing-speed', name: 'Typing Speed Calculator', desc: 'Gross and net WPM from characters and errors', kw: 'typing speed calculator wpm accuracy',
    inputs: [{id:'chars',label:'Characters Typed',type:'number',def:500},{id:'minutes',label:'Minutes',type:'number',def:2},{id:'errors',label:'Errors',type:'number',def:10}],
    calc: function(v){ if(v.minutes<=0) return{result:'Minutes must be > 0',chart:'',extra:''}; var gross=v.chars/5/v.minutes; var net=Math.max(0,gross-v.errors/v.minutes); var acc=v.chars>0?(1-v.errors/v.chars)*100:0; return{result:'Net: '+net.toFixed(1)+' WPM',chart:Charts.bar([gross,net],['Gross','Net']),extra:'Gross: '+gross.toFixed(1)+' WPM | Accuracy: '+acc.toFixed(1)+'% | Standard word = 5 characters'};},
    steps: function(v){ var gross=v.chars/5/v.minutes; var net=Math.max(0,gross-v.errors/v.minutes); return['Step 1: Gross WPM = '+v.chars+' chars / 5 / '+v.minutes+' min = '+gross.toFixed(1),'Step 2: Subtract errors: − '+v.errors+'/'+v.minutes+' = '+net.toFixed(1)+' WPM net'];} },

  { id: 'ad-revenue', name: 'Ad Revenue Calculator', desc: 'CPM/CPC ad earnings estimate', kw: 'ad revenue calculator cpm cpc impressions',
    inputs: [{id:'impressions',label:'Monthly Impressions',type:'number',def:100000},{id:'cpm',label:'CPM ($ per 1000)',type:'number',def:5},{id:'ctr',label:'Click-Through Rate (%)',type:'number',def:1},{id:'cpc',label:'CPC ($ per click)',type:'number',def:0.3}],
    calc: function(v){ var revCpm=v.impressions/1000*v.cpm; var clicks=v.impressions*v.ctr/100; var revCpc=clicks*v.cpc; return{result:'CPM revenue: $'+revCpm.toFixed(2)+'/mo',chart:Charts.bar([revCpm,revCpc],['CPM','CPC']),extra:'CPC revenue: $'+revCpc.toFixed(2)+' | Clicks: '+clicks.toFixed(0)+' | Estimate only — fill rates vary'};},
    steps: function(v){ var revCpm=v.impressions/1000*v.cpm; var clicks=v.impressions*v.ctr/100; var revCpc=clicks*v.cpc; return['Step 1: CPM revenue = '+v.impressions.toLocaleString()+' / 1000 × $'+v.cpm+' = $'+revCpm.toFixed(2),'Step 2: Clicks = '+v.impressions.toLocaleString()+' × '+v.ctr+'% = '+clicks.toFixed(0),'Step 3: CPC revenue = '+clicks.toFixed(0)+' × $'+v.cpc+' = $'+revCpc.toFixed(2)];} },

  { id: 'click-through-rate', name: 'Click-Through Rate Calculator', desc: 'CTR from clicks and impressions', kw: 'click through rate calculator ctr clicks impressions',
    inputs: [{id:'clicks',label:'Clicks',type:'number',def:2500},{id:'impressions',label:'Impressions',type:'number',def:100000}],
    calc: function(v){ if(v.impressions<=0) return{result:'Impressions must be > 0',chart:'',extra:''}; var ctr=v.clicks/v.impressions*100; return{result:'CTR: '+ctr.toFixed(2)+'%',chart:Charts.gauge(ctr,10),extra:v.clicks.toLocaleString()+' clicks / '+v.impressions.toLocaleString()+' impressions | Benchmarks vary by channel (0.2–3%)'};},
    steps: function(v){ var ctr=v.clicks/v.impressions*100; return['Formula: CTR = Clicks / Impressions × 100','Step 1: '+v.clicks.toLocaleString()+' / '+v.impressions.toLocaleString()+' = '+(v.clicks/v.impressions).toFixed(5),'Step 2: CTR = '+ctr.toFixed(2)+'%'];} },

  { id: 'subnet-cidr', name: 'Subnet Calculator (CIDR)', desc: 'Network, broadcast, mask and host count from CIDR', kw: 'subnet calculator cidr ipv4 netmask broadcast hosts network range',
    inputs: [{id:'ip',label:'IPv4 Address',type:'text',def:'192.168.1.10'},{id:'prefix',label:'Prefix (/bits)',type:'number',def:26}],
    calc: function(v){ var o=v.ip.split('.').map(Number); if(o.length!==4||o.some(isNaN)||o.some(function(x){return x<0||x>255;})||v.prefix<0||v.prefix>32) return{result:'Enter a valid IPv4 address and a prefix of 0–32',chart:'',extra:''}; var ip=((o[0]<<24)|(o[1]<<16)|(o[2]<<8)|o[3])>>>0; var mask=v.prefix===0?0:((0xFFFFFFFF<<(32-v.prefix))>>>0); var net=(ip&mask)>>>0; var bc=(net|(~mask>>>0))>>>0; function s(n){return [(n>>>24)&255,(n>>>16)&255,(n>>>8)&255,n&255].join('.');} var total=Math.pow(2,32-v.prefix); var hosts=v.prefix>=31?total:(total-2); return{result:'Network: '+s(net)+'/'+v.prefix,chart:Charts.bar([total,hosts],['Total addresses','Usable hosts']),extra:'Mask: '+s(mask)+' | Broadcast: '+s(bc)+' | Usable range: '+s(v.prefix>=31?net:net+1)+' – '+s(v.prefix>=31?bc:bc-1)+' ('+hosts+' hosts)'};},
    steps: function(v){ var o=v.ip.split('.').map(Number); var ip=((o[0]<<24)|(o[1]<<16)|(o[2]<<8)|o[3])>>>0; var mask=v.prefix===0?0:((0xFFFFFFFF<<(32-v.prefix))>>>0); var net=(ip&mask)>>>0; function s(n){return [(n>>>24)&255,(n>>>16)&255,(n>>>8)&255,n&255].join('.');} var total=Math.pow(2,32-v.prefix); return['Formula: mask = 32−prefix host bits set to 0; network = IP AND mask','Step 1: /'+v.prefix+' leaves '+(32-v.prefix)+' host bits → '+total+' addresses','Step 2: Mask = '+s(mask),'Step 3: Network = '+s(net)+'; subtract 2 (network + broadcast) for usable hosts unless /31 or /32'];} },

  { id: 'video-bitrate', name: 'Video File Size Calculator', desc: 'Storage needed from bitrate and duration', kw: 'video file size calculator bitrate duration mb gb streaming upload estimate',
    inputs: [{id:'mbps',label:'Video Bitrate (Mbps)',type:'number',def:8},{id:'min',label:'Duration (minutes)',type:'number',def:10},{id:'audio',label:'Audio Bitrate (kbps)',type:'number',def:128}],
    calc: function(v){ if(v.mbps<=0||v.min<=0) return{result:'Bitrate and duration must be positive',chart:'',extra:''}; var bits=(v.mbps*1e6+v.audio*1000)*v.min*60; var MB=bits/8/1e6; return{result:'File Size: '+MB.toFixed(1)+' MB',chart:Charts.bar([v.mbps*1e6,v.audio*1000],['Video (bps)','Audio (bps)']),extra:'Total bitrate: '+((v.mbps*1e6+v.audio*1000)/1e6).toFixed(3)+' Mbps | In GB: '+(MB/1000).toFixed(3)+' | Per hour at these settings: '+(MB*6/1000).toFixed(2)+' GB'};},
    steps: function(v){ var bits=(v.mbps*1e6+v.audio*1000)*v.min*60; var MB=bits/8/1e6; return['Formula: size = (video + audio bitrate) × seconds ÷ 8','Step 1: Total bitrate = '+v.mbps+' Mbps + '+(v.audio/1000)+' Mbps = '+((v.mbps*1e6+v.audio*1000)/1e6).toFixed(3)+' Mbps','Step 2: Bits = '+((v.mbps*1e6+v.audio*1000)/1e6).toFixed(3)+'e6 × '+(v.min*60)+' s = '+bits.toExponential(3),'Step 3: ÷ 8 → bytes = '+MB.toFixed(1)+' MB','Container overhead adds roughly 1–2% beyond the raw stream size'];} },

  { id: 'cidr-convert', name: 'CIDR to Subnet Calculator', desc: 'Mask, host count, and broadcast details for a CIDR block', kw: 'cidr calculator subnet mask hosts broadcast ipv4 network', inputs: [{id:'prefix',label:'CIDR Prefix (0-32)',type:'number',def:24},{id:'octet',label:'Network Octet Check (e.g. 10.0.1.x -> enter 1)',type:'number',def:1}], calc: function(v){ if(v.prefix<0||v.prefix>32) return{result:'Prefix must be between 0 and 32',chart:'',extra:''}; var total=Math.pow(2,32-v.prefix); var usable=total-2; if(v.prefix>=31) usable=total; var maskBits=32-v.prefix; var maskOct=[]; for(var i=0;i<4;i++){var bits=Math.max(0,Math.min(8,maskBits-i*8)); var val=bits===0?0:256-Math.pow(2,8-bits); maskOct.push(val);} return{result:'/'+v.prefix+' = '+maskOct.join('.')+' ('+usable+' usable hosts)',chart:Charts.gauge(v.prefix,32),extra:'Total addresses: '+total.toLocaleString()+' | /'+(v.prefix>=31?'no broadcast reserved for point-to-point links':'network + broadcast use 2, leaving '+usable.toLocaleString())} ;}, steps: function(v){ var total=Math.pow(2,32-v.prefix); return['Step 1: Host bits = 32 − '+v.prefix+' = '+(32-v.prefix),'Step 2: Total addresses = 2^'+(32-v.prefix)+' = '+total.toLocaleString(),'Step 3: Usable = '+Math.max(0,total-2).toLocaleString()+' (minus network and broadcast)','Step 4: Mask = 32-bit value with '+v.prefix+' ones followed by zeros'];} },

  { id: 'gpu-comparison-perf', name: 'GPU Value Comparison', desc: 'Frames per second per dollar and per watt', kw: 'gpu comparison calculator fps per dollar performance watt gaming value', inputs: [{id:'fps',label:'Benchmark FPS',type:'number',def:90},{id:'price',label:'Card Price ($)',type:'number',def:500},{id:'watt',label:'Board Power (W)',type:'number',def:250}], calc: function(v){ if(v.price<=0||v.watt<=0||v.fps<0) return{result:'Enter positive price and power',chart:'',extra:''}; var fpd=v.fps/v.price; var fpw=v.fps/v.watt; return{result:fpd.toFixed(3)+' FPS per $',chart:Charts.bar([fpd*1000,fpw*10],['FPS/$ ×1000','FPS/W ×10']),extra:'Performance per watt: '+fpw.toFixed(3)+' FPS/W | Over 3 years at $0.15/kWh the card burns about $'+(v.watt*24*365*3/1000*0.15).toFixed(0)+' of electricity — factor it into total cost'};}, steps: function(v){ var fpd=v.fps/v.price; var fpw=v.fps/v.watt; return['Step 1: FPS per dollar = '+v.fps+' ÷ $'+v.price+' = '+fpd.toFixed(4),'Step 2: FPS per watt = '+v.fps+' ÷ '+v.watt+' W = '+fpw.toFixed(4),'Step 3: Compare both — a cheap card that draws more power can cost more per frame over its life','Use the same resolution and game settings for every card in the comparison'];} },

  { id: 'data-center-pue', name: 'Data Center PUE Calculator', desc: 'Power usage effectiveness from facility and IT load', kw: 'pue calculator data center power usage effectiveness efficiency', inputs: [{id:'total',label:'Total Facility Power (kW)',type:'number',def:130},{id:'it',label:'IT Equipment Power (kW)',type:'number',def:100}], calc: function(v){ if(v.it<=0||v.total<=0) return{result:'Both values must be positive',chart:'',extra:''}; var pue=v.total/v.it; var band=pue<=1.2?'excellent':pue<=1.4?'good':pue<=1.8?'average':'poor'; return{result:'PUE: '+pue.toFixed(2)+' ('+band+')',chart:Charts.gauge(pue,3),extra:'Overhead: '+((pue-1)*100).toFixed(0)+'% of IT power goes to cooling, UPS losses, and lighting — every 0.1 reduction saves roughly '+(v.it*0.1*8760/1000).toFixed(0)+' MWh per year at this scale'};}, steps: function(v){ var pue=v.total/v.it; return['Formula: PUE = Total facility power ÷ IT equipment power','Step 1: '+v.total+' kW ÷ '+v.it+' kW = '+pue.toFixed(3),'Step 2: Overhead = (PUE − 1) × 100 = '+((pue-1)*100).toFixed(0)+'%','PUE of 1.0 is the theoretical floor — hyperscale operators reach 1.1, older enterprise halls often sit at 2.0'];} },

  { id: 'screen-ppi', name: 'Screen PPI Calculator', desc: 'Pixels per inch from resolution and diagonal', kw: 'ppi calculator pixels per inch screen density resolution diagonal', inputs: [{id:'w',label:'Width (px)',type:'number',def:1920},{id:'h',label:'Height (px)',type:'number',def:1080},{id:'d',label:'Diagonal (inches)',type:'number',def:24}], calc: function(v){ if(v.d<=0||v.w<=0||v.h<=0) return{result:'Enter positive dimensions',chart:'',extra:''}; var dp=Math.sqrt(v.w*v.w+v.h*v.h); var ppi=dp/v.d; return{result:'PPI: '+ppi.toFixed(1),chart:Charts.bar([ppi,300],['this screen','retina-ish 300']),extra:'Total pixels: '+(v.w*v.h).toLocaleString()+' | Pixel pitch '+((25.4/ppi)).toFixed(3)+' mm — under about 40 cm viewing distance, 90+ PPI hides individual pixels'};}, steps: function(v){ var dp=Math.sqrt(v.w*v.w+v.h*v.h); var ppi=dp/v.d; return['Step 1: Diagonal in pixels = √(w² + h²) = √('+(v.w*v.w)+' + '+(v.h*v.h)+') = '+dp.toFixed(1),'Step 2: PPI = '+dp.toFixed(1)+' ÷ '+v.d+' in = '+ppi.toFixed(1),'Compare PPI at your real viewing distance — phones need 300+, laptops 150+, TVs look fine under 100'];} },

  { id: 'hash-rate', name: 'Mining Yield Calculator', desc: 'Expected coin yield from network share of hashrate', kw: 'hashrate calculator mining yield bitcoin crypto block reward', inputs: [{id:'hr',label:'Your Hashrate (TH/s)',type:'number',def:100},{id:'net',label:'Network Hashrate (EH/s)',type:'number',def:500},{id:'reward',label:'Block Reward (coins)',type:'number',def:3.125},{id:'price',label:'Coin Price ($)',type:'number',def:60000}], calc: function(v){ if(v.net<=0||v.hr<=0) return{result:'Enter positive hashrates',chart:'',extra:''}; var share=v.hr*1e12/(v.net*1e18); var blocksPerDay=144; var coins=share*blocksPerDay*v.reward; return{result:coins.toFixed(8)+' coins/day',chart:Charts.bar([coins*v.price],['$ per day']),extra:'Network share: '+(share*100).toExponential(2)+'% | ≈ $'+(coins*v.price).toFixed(2)+'/day before power costs — subtract your kWh rate × rig watts × 24 ÷ 1000 × price'};}, steps: function(v){ var share=v.hr*1e12/(v.net*1e18); var coins=share*144*v.reward; return['Step 1: Your share = '+v.hr+' TH/s ÷ '+(v.net*1000)+' TH/s network = '+(share*100).toExponential(3)+'%','Step 2: Blocks per day ≈ 144 (Bitcoin 10-minute target)','Step 3: Coins/day = share × 144 × reward = '+coins.toFixed(8),'Variance is enormous for small miners — most pool solo; consider pool payouts for smooth income'];} },

  { id: 'storage-array', name: 'RAID Usable Capacity Calculator', desc: 'Usable and fault-tolerant capacity for common RAID levels', kw: 'raid calculator usable capacity raid 5 raid 10 storage array', inputs: [{id:'n',label:'Number of Disks',type:'number',def:5},{id:'tb',label:'Disk Size (TB)',type:'number',def:8},{id:'raid',label:'RAID level (0, 1, 5, 6, 10)',type:'number',def:5}], calc: function(v){ if(v.n<2||v.tb<=0) return{result:'Need at least 2 disks of positive size',chart:'',extra:''}; var usable; var tol; if(v.raid===0){usable=v.n*v.tb;tol=0;} else if(v.raid===1){usable=v.tb;tol=Math.floor(v.n/2);} else if(v.raid===5){if(v.n<3) return{result:'RAID 5 needs at least 3 disks',chart:'',extra:''}; usable=(v.n-1)*v.tb;tol=1;} else if(v.raid===6){if(v.n<4) return{result:'RAID 6 needs at least 4 disks',chart:'',extra:''}; usable=(v.n-2)*v.tb;tol=2;} else if(v.raid===10){if(v.n%2) return{result:'RAID 10 needs an even disk count',chart:'',extra:''}; usable=v.n/2*v.tb;tol=Math.floor(v.n/2);} else return{result:'Supported levels: 0, 1, 5, 6, 10',chart:'',extra:''}; return{result:'Usable: '+usable.toFixed(0)+' TB',chart:Charts.bar([v.n*v.tb,usable],['Raw','Usable']),extra:'Fault tolerance: '+tol+' disk'+(tol===1?'':'s')+' | Raw capacity '+(v.n*v.tb).toFixed(0)+' TB, efficiency '+((usable/(v.n*v.tb))*100).toFixed(0)+'%'};}, steps: function(v){ return['Step 1: Raw = '+v.n+' × '+v.tb+' TB = '+(v.n*v.tb).toFixed(0)+' TB','Step 2: RAID '+v.raid+' reserves parity or mirrors per its rule','Step 3: Usable formula — RAID 0: n×d, RAID 1: 1×d, RAID 5: (n−1)×d, RAID 6: (n−2)×d, RAID 10: (n/2)×d','Rebuild time matters: large drives can take a day to rebuild, during which a second failure is survivable only in RAID 6'];} },

  { id: 'docker-resource', name: 'Container Density Calculator', desc: 'How many containers fit given RAM and CPU budgets', kw: 'docker container density calculator ram cpu limit kubernetes pods per node', inputs: [{id:'ram',label:'Host RAM (GB)',type:'number',def:16},{id:'cpu',label:'Host vCPUs',type:'number',def:8},{id:'cram',label:'RAM per Container (GB)',type:'number',def:1},{id:'ccpu',label:'vCPU per Container',type:'number',def:0.5},{id:'over',label:'Reserve for OS (%)',type:'number',def:20}], calc: function(v){ if(v.cram<=0||v.ccpu<=0) return{result:'Per-container limits must be positive',chart:'',extra:''}; var ramAv=v.ram*(1-v.over/100); var byRam=Math.floor(ramAv/v.cram); var byCpu=Math.floor(v.cpu/v.ccpu); var n=Math.min(byRam,byCpu); var lim=byRam<byCpu?'RAM':'CPU'; return{result:n+' containers (limited by '+lim+')',chart:Charts.bar([byRam,byCpu],['RAM limit','CPU limit']),extra:'Usable RAM: '+ramAv.toFixed(1)+' GB after the OS reserve | Leave headroom for bursts — running at 100% of the estimate invites OOM kills'};}, steps: function(v){ var ramAv=v.ram*(1-v.over/100); var byRam=Math.floor(ramAv/v.cram); var byCpu=Math.floor(v.cpu/v.ccpu); return['Step 1: Usable RAM = '+v.ram+' × '+(1-v.over/100)+' = '+ramAv.toFixed(1)+' GB','Step 2: RAM-bound count = floor('+ramAv.toFixed(1)+' ÷ '+v.cram+') = '+byRam,'Step 3: CPU-bound count = floor('+v.cpu+' ÷ '+v.ccpu+') = '+byCpu,'Step 4: The smaller of the two wins — '+Math.min(byRam,byCpu)+' containers'];} },

  { id: 'load-test-users', name: 'Load Test VU Calculator', desc: 'Virtual users needed to hit a target request rate', kw: 'load test virtual users calculator vu target rps response time', inputs: [{id:'rps',label:'Target Requests/sec',type:'number',def:500},{id:'rt',label:'Avg Response Time (s)',type:'number',def:0.4},{id:'think',label:'Think Time (s)',type:'number',def:0}], calc: function(v){ if(v.rps<=0||v.rt<0) return{result:'Enter a positive RPS target',chart:'',extra:''}; var vu=v.rps*(v.rt+v.think); return{result:Math.ceil(vu)+' virtual users',chart:Charts.bar([v.rt,v.think],['Response','Think']),extra:'Little-s law: VUs = RPS × (response + think time) | Ramp gradually — jumping straight to peak hides queueing clues in your metrics'};}, steps: function(v){ var vu=v.rps*(v.rt+v.think); return['Formula (Little-s law): VU = RPS × (response time + think time)','Step 1: ('+v.rt+' + '+v.think+') = '+(v.rt+v.think)+' s per iteration','Step 2: '+v.rps+' × '+(v.rt+v.think)+' = '+vu.toFixed(1)+' → ceil to '+Math.ceil(vu),'If actual RPS falls short, the system is queueing — raise VUs only after fixing server-side saturation'];} },
  { id: 'wifi-throughput', name: 'Wi-Fi Real Throughput Calculator', desc: 'Expected usable throughput from link speed and efficiency', kw: 'wifi throughput calculator link speed real transfer rate overhead wireless',
    inputs: [{id:'link',label:'Link Speed (Mb/s)',type:'number',def:100},{id:'eff',label:'Efficiency Factor (0–1)',type:'number',def:0.6}],
    calc: function(v){ const tp=v.link*v.eff; return { result: tp.toFixed(1)+' Mb/s', chart: null, extra: 'Real throughput ≈ link rate × efficiency (0.5–0.7 typical for Wi-Fi) — protocol overhead, retries, and interference eat the rest' }; },
    steps: function(v){ return ['Throughput = link speed × efficiency']; } },
  { id: 'data-usage-est', name: 'Data Usage Estimator', desc: 'Monthly mobile data from daily streaming hours', kw: 'data usage estimator calculator monthly mobile gb streaming plan',
    inputs: [{id:'hrs',label:'Streaming Hours per Day',type:'number',def:2},{id:'gbph',label:'Data per Hour (GB)',type:'number',def:3},{id:'days',label:'Days per Month',type:'number',def:30}],
    calc: function(v){ const gb=v.hrs*v.gbph*v.days; return { result: gb.toFixed(0)+' GB', chart: null, extra: 'Typical rates: SD ~0.7 GB/h, HD ~3 GB/h, 4K ~7 GB/h per stream (video quality setting dominates)' }; },
    steps: function(v){ return ['GB = hours/day × GB/hour × days']; } },
  { id: 'image-size-calc', name: 'Uncompressed Image Size Calculator', desc: 'Raw bitmap size from pixel dimensions and channels', kw: 'image size calculator uncompressed bitmap bytes pixels bit depth raw file',
    inputs: [{id:'w',label:'Width (px)',type:'number',def:4000},{id:'h',label:'Height (px)',type:'number',def:3000},{id:'ch',label:'Bytes per Pixel (3 = RGB 8-bit)',type:'number',def:3}],
    calc: function(v){ const bytes=v.w*v.h*v.ch; const mb=bytes/(1024*1024); return { result: mb.toFixed(1)+' MB', chart: null, extra: bytes.toLocaleString()+' bytes raw. JPEG/PNG compress this substantially; 16-bit channels double the bytes-per-pixel' }; },
    steps: function(v){ return ['Bytes = width × height × bytes/pixel','MB = bytes ÷ 1,048,576']; } },
];
if (typeof module !== 'undefined') module.exports = TECH_TOOLS;
if (typeof window !== 'undefined') window.TECH_TOOLS = TECH_TOOLS;

